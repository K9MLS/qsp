# Source assets

Empty. Reserved for source material that is processed into `console/static/` —
icon sources, an original logo, font licences if a webfont is ever self-hosted.

Everything in `console/static/` is served verbatim and embedded into the binary.
Nothing here is served.

There is no build step (ADR-0001), so anything placed here is converted by hand
and the conversion documented.
